#include <stdio.h>
int main()
{
    int length,breadth;
    scanf("%d %d",&lenght, &breadth);

    for(int i=1;i<=length;i=i+1)
    {
        for(int j=1;j<=breadth;j=j+1)
        {
            printf("\n")
            printf("*");
        }

    }
}