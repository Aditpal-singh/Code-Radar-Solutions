#include <stdio.h>

int main() 
{   
    char x;
    scanf("%c",x);
    if(isupperx)
    {
        printf("Uppercase");
    }
    else if(islowerx)
    {
        printf("Lowercase");
    }
    else
    {
        printf("Not alphabet");
    }
    printf("%s", welcome());
    return 0;
}